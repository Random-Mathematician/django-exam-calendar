[project]
name = "exam-calendar-vercel-django"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
  "python-dotenv",
  "psycopg>=3.0",
  "django>=6.0"
]

[tool.vercel]
entrypoint = "examcalendar.wsgi:application"